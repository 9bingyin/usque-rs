# usque-rs 开发规范 (SPEC)

> **Note**: 本文件 (`.rules`) 是项目的核心规范文档。`CLAUDE.md`、`AGENTS.md`、`GEMINI.md` 等 AI 配置文件均可软链接至此文件。

## 1. 项目概述

usque-rs 是一个基于 Cloudflare WARP 的 MASQUE (Multiplexed Application Substrate over QUIC Encryption) 代理客户端，使用 Rust 实现。

### 1.1 核心功能

- **SOCKS5 代理服务器**：支持 CONNECT、BIND、UDP ASSOCIATE 命令
- **MASQUE 隧道**：通过 QUIC/HTTP3 协议与 Cloudflare 建立加密隧道
- **用户态协议栈**：基于 smoltcp 实现的 TCP/IP 协议栈，无需系统权限
- **设备注册**：与 Cloudflare API 交互，自动注册设备并获取配置

### 1.2 技术架构

```
SOCKS5 Client <-> SOCKS5 Server <-> smoltcp (TCP/IP) <-> MASQUE Tunnel <-> QUIC/H3 <-> Cloudflare
```

数据流：
1. SOCKS5 客户端发送连接请求
2. SOCKS5 服务器通过 TunnelManager 创建 TCP 连接
3. smoltcp 协议栈生成 IP 包
4. MASQUE 隧道将 IP 包封装为 HTTP/3 Datagram
5. QUIC 连接将数据发送到 Cloudflare 服务器

---

## 2. 开发环境与工具

### 2.1 构建工具

- **Rust 工具链**：使用 nightly 版本（项目使用 `let-chains` 等实验特性）
- **构建命令**：
  - `cargo build --release` - 构建发布版本
  - `cargo clippy` - 静态分析
  - `cargo fmt` - 代码格式化
  - `cargo test` - 运行测试

### 2.2 依赖说明

| 依赖 | 用途 |
|------|------|
| `tokio` | 异步运行时 |
| `quiche` | QUIC/HTTP3 协议实现 |
| `smoltcp` | 用户态 TCP/IP 协议栈 |
| `hickory-proto` | DNS 协议解析 |
| `fast-socks5` | SOCKS5 协议处理 |
| `p256` | ECDSA 密钥操作 |
| `boring` / `ring` | TLS/加密原语 |

---

## 3. 项目结构

```
src/
  main.rs          # CLI 入口，命令行参数解析
  lib.rs           # 库入口
  config.rs        # 配置文件管理（JSON 格式）
  api/
    mod.rs         # API 模块导出
    cloudflare.rs  # Cloudflare API 客户端
    models.rs      # API 数据模型
  crypto/
    mod.rs         # 加密模块导出
    keys.rs        # 密钥生成、证书生成
  tunnel/
    mod.rs         # 隧道模块导出
    quic.rs        # QUIC 连接管理（quiche）
    masque.rs      # MASQUE 隧道（HTTP/3 CONNECT-IP）
    stack.rs       # 用户态 TCP/IP 协议栈（smoltcp）
    device.rs      # 虚拟网络设备
    dns.rs         # DNS 查询与解析
    manager.rs     # 隧道管理器（连接池、重连、数据转发）
  proxy/
    mod.rs         # 代理模块导出
    socks5.rs      # SOCKS5 服务器实现
```

---

## 4. 核心模块规范

### 4.1 tunnel/manager.rs - 隧道管理器

**职责**：
- 管理 MASQUE 隧道连接的生命周期
- 处理 SOCKS5 连接与隧道之间的数据转发
- DNS 解析与缓存
- 断线重连（指数退避 + 抖动）

**关键组件**：

| 组件 | 说明 |
|------|------|
| `TunnelManager` | 单个隧道管理器 |
| `TunnelManagerPool` | 隧道池，支持多 worker |
| `ManagerCommand` | 命令通道消息类型 |
| `RuntimeState` | 运行时状态（协议栈、socket 映射、DNS 缓存等）|

**设计原则**：
- 所有与隧道的交互通过 `ManagerCommand` 命令通道进行
- 使用事件驱动模式而非轮询（`wait_socket_ready`）
- DNS 查询实现 Happy Eyeballs（同时发送 A/AAAA 查询）
- 反压控制：每个 socket 有独立的发送/接收缓冲区限制（`MAX_PENDING_DATA`）

### 4.2 tunnel/masque.rs - MASQUE 隧道

**职责**：
- 实现 Cloudflare 的 `cf-connect-ip` 协议
- HTTP/3 Datagram 封装/解封装
- IP 包处理（TTL 递减、校验和计算）
- ICMP Packet Too Big 生成

**关键常量**：
- `CONTEXT_ID_ZERO = 0x00` - IP 包的 Context ID（RFC 9484）
- `MIN_MTU = 1280` - 最小 MTU

### 4.3 tunnel/quic.rs - QUIC 连接

**职责**：
- QUIC 连接建立与维护
- TLS 证书配置
- 服务器公钥固定（Public Key Pinning）

**配置参数**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `idle_timeout` | 90000ms | QUIC 空闲超时 |
| `congestion_control` | CUBIC | 拥塞控制算法 |
| `initial_packet_size` | 1242 | 初始包大小 |
| `enable_dgram` | true | 启用 DATAGRAM 扩展 |

### 4.4 tunnel/stack.rs - 协议栈

**职责**：
- smoltcp 接口封装
- TCP/UDP socket 管理
- IP 地址配置

**设计原则**：
- 使用 `valid_handles` 集合跟踪有效 socket，防止访问已移除的 socket
- 所有 socket 操作需先检查 handle 有效性

### 4.5 proxy/socks5.rs - SOCKS5 服务器

**职责**：
- SOCKS5 协议握手与认证
- TCP CONNECT 命令处理
- TCP BIND 命令处理
- UDP ASSOCIATE 命令处理
- 数据转发

**关键参数**：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `CONNECT_TIMEOUT` | 30s | TCP 连接超时 |
| `IDLE_TIMEOUT` | 300s | 连接空闲超时 |
| `CONNECTION_ATTEMPT_DELAY` | 250ms | Happy Eyeballs 连接间隔 |

**Happy Eyeballs 实现**（RFC 8305）：
- 解析域名时同时获取所有地址
- IPv6/IPv4 地址交错排序
- 并发连接尝试，间隔 250ms
- 首个成功连接获胜，关闭其他连接

---

## 5. 代码规范

### 5.1 通用原则

- **代码正确性和清晰度优先**，性能和效率是次要考虑（除非明确要求）
- **优先在现有文件中实现功能**，除非是全新的逻辑组件，避免创建过多小文件
- **避免创造性添加**，除非明确要求
- **不要写总结代码的注释**，注释只用于解释"为什么"代码要这样写（当原因不明显时）

### 5.2 命名规范

- 变量名使用完整单词，禁止缩写（如用 `queue` 而不是 `q`）
- 错误类型命名：`{Module}Error`（如 `Socks5Error`、`MasqueError`）

### 5.3 错误处理

- 每个模块定义独立的 Error 枚举（使用 `thiserror`）
- 使用 `?` 操作符传播错误，避免嵌套 `match`
- **禁止使用 `unwrap()`**，使用 `?` 传播错误或用 `expect()` 提供上下文
- 小心可能 panic 的操作（如数组索引越界）
- **永远不要用 `let _ =` 静默丢弃可失败操作的错误**，必须正确处理：
  - 使用 `?` 向上传播错误
  - 使用 `.ok()` 或 `if let Err(e) = ...` 显式忽略并记录日志
  - 使用 `match` 或 `if let Err(...)` 进行自定义处理

### 5.4 日志规范

- 使用 `log` crate（通过 `env_logger` 输出）
- 日志级别：
  - `error!` - 不可恢复的错误
  - `warn!` - 可恢复的错误、异常情况
  - `info!` - 重要状态变化（连接建立、配置加载）
  - `debug!` - 调试信息（DNS 解析、连接状态）
  - `trace!` - 详细追踪（数据包收发、channel 关闭）

### 5.5 异步编程

- 使用 `tokio` 运行时
- 通道选择：
  - 命令通道：`mpsc::channel`（有界）
  - 一次性响应：`oneshot::channel`
- `tokio::select!` 中使用 `biased;` 优先处理高优先级分支
- 避免在 `select!` 中持有跨 `.await` 的锁
- **异步闭包中使用变量影子（shadowing）限制 clone 的生命周期**：
  ```rust
  executor.spawn({
      let state = state.clone();
      async move {
          // 使用 state
      }
  });
  ```

### 5.6 并发安全

- 使用 `Arc` 共享只读数据
- 避免全局可变状态
- 原子操作使用 `std::sync::atomic`（端口计数器、DNS transaction ID 等）
- 单线程 manager 内部状态无需加锁

### 5.7 内存管理

- 使用 `bytes::Bytes` 进行零拷贝数据传递
- `VirtualDevice` 实现缓冲区池复用
- 避免在热路径上分配大型 Vec

### 5.8 文件组织

- **禁止创建 `mod.rs`** - 使用 `src/some_module.rs` 而非 `src/some_module/mod.rs`
- 新建 crate 时，在 `Cargo.toml` 中使用 `[lib] path = "..."` 指定库入口文件，而非默认的 `lib.rs`

---

## 6. 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `USQUE_CC` | `cubic` | 拥塞控制算法（reno/cubic/bbr2）|
| `USQUE_TCP_BUFFER_SIZE` | `1048576` | TCP 缓冲区大小（字节）|
| `USQUE_QUIC_IDLE_TIMEOUT_MS` | `90000` | QUIC 空闲超时（毫秒）|
| `USQUE_TUNNEL_WORKERS` | `1` | 隧道 worker 数量 |
| `USQUE_MAX_CONNECTIONS` | `1024` | 最大并发连接数 |
| `RUST_LOG` | - | 日志级别（如 `usque_rs=debug`）|

---

## 7. 安全规范

### 7.1 密钥管理

- ECDSA 私钥使用 SEC1 格式存储（与 Go 版本兼容）
- 配置文件中的私钥为 Base64 编码
- 运行时生成的自签名证书有效期为 24 小时

### 7.2 TLS/QUIC 安全

- 服务器公钥固定（SPKI 格式）
- 禁用 GREASE（`config.grease(false)`）以避免兼容性问题
- 禁用证书验证（`verify_peer(false)`）但启用公钥固定

### 7.3 输入验证

- DNS 响应验证 transaction ID
- IP 包版本验证（只处理 IPv4/IPv6）
- SOCKS5 参数校验

---

## 8. 性能优化要点

### 8.1 数据转发

- 使用事件驱动而非轮询检查 socket 状态
- 批量读取 TCP 数据（`MAX_TCP_READ_CHUNK = 64KB`）
- 反压控制防止内存无限增长

### 8.2 DNS

- DNS 缓存（容量 1024 条）
- 缓存条目带 TTL 过期
- 负载均衡：随机选择缓存中的地址

### 8.3 连接管理

- 隧道池支持多 worker 并行处理
- 断线重连使用指数退避（1s - 60s）+ 抖动（50%）
- Keepalive PING 保持连接活跃
